import React, { Component } from 'react';

class Shop extends Component {
  render() { 
    return (
      <div>
        <h2>This is shopping page...Available only for authenticated users.</h2>
      </div>
    );
  }
}

export default Shop;